# 공통 기본값 (운영 기준)
REDIS_URL=redis://redis-master.redis.svc.cluster.local:6379

# 앱별 prefix (필수)
NEXT_CACHE_KEY_PREFIX=app-a:

# Redis 캐시 ON
NEXT_CACHE_REDIS_ENABLED=1

# 연결 타임아웃
REDIS_CONNECT_TIMEOUT_MS=5000

# Next 캐시 엔트리 TTL (0 = Next가 관리)
NEXT_CACHE_ENTRY_TTL_SECONDS=0
